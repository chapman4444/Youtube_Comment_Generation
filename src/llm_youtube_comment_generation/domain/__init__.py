"""Pure product rules.

Nothing in this package may import a third-party library, an adapter, or an
interface. That is not a convention: ``tests/contract/test_import_direction.py``
parses every module here and fails the build when it happens.
"""
