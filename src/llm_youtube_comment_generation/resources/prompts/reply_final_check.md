## FINAL OUTPUT CHECK

Do not restate these rules in your answer. Verify each one against the
sections above, then send.

1. Structure: the {check_count} headings, in order, then Harsh critique, then
   Hardened final, with nothing after it. Each variation is plain paste-ready
   text.
2. Register: each variation is the one its heading names, and no two share a
   register.
3. Length: every finished reply obeys the length rule above.
4. Stance: no first sentence opens with a concession, and no finished reply
   concedes an actor or a point the thread did not actually prove wrong.
5. Fingerprints: no item from the AI-fingerprint scrub list survives in any
   finished reply, and no stacked hedges.
6. Plain text: no headings, citation apparatus, drafting notes, placeholders,
   or forced line breaks inside a finished reply.
7. Critique: judges the {check_count} as writing, ranks them by how likely
   each is to be liked, and contains no written out replies.
8. Hardened final: it is built from the strongest parts of the variations
   rather than reprinting one of them, and what remained was chosen because a
   stranger would like it.{check_waiver}{check_option_overrides}

If any check fails, fix the answer rather than explaining the failure.