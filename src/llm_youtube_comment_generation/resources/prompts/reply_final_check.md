## FINAL OUTPUT CHECK

Do not restate these rules in your answer. Verify each one against the
sections above, then send.

1. Sheet structure: the chat response begins with # Copy/Paste Replies, then
   Direct replies to your comment, then Nested replies between other users,
   then Relationship unresolved only when an unresolved target exists.
   Within each section the targets keep their original response order.
2. Coverage: every target appears exactly once across the sheet under its
   Response heading, with its own Post beneath comment ID, Author channel
   ID, Responding to, and Relationship lines. No target is missing,
   duplicated, merged, or invented, and every comment id is copied exactly.
3. Code blocks: each target has exactly one text code block holding only
   its finished paste-ready reply. No ids, headings, labels, analysis,
   alternatives, or wrapping quotation marks inside any block, and never
   two replies in one block.
4. Register: in each target's audit file, the {check_count} variations are
   each the one its heading names, no two share a register, and the
   critique ranks them by how likely each is to be liked without writing
   out new replies.
5. Length: every finished reply obeys the length rules above.
6. Stance: no first sentence opens with a concession, and no finished reply
   concedes an actor or a point the thread did not actually prove wrong.
7. Fingerprints: no item from the AI-fingerprint scrub list survives in any
   finished reply, and no stacked hedges.
8. Audit package: one audit file per target with the required sections, its
   Hardened final identical to that target's code block in the sheet, plus
   COPY_PASTE_RESPONSES.md, reply_index.md, README.md, and the ZIP holding
   them all — delivered after the sheet, with nothing between the sheet and
   the files.{check_waiver}{check_option_overrides}

If any check fails, fix the answer rather than explaining the failure.