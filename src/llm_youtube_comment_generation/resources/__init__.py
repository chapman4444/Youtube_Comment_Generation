"""Versioned prompt text and declarative option definitions.

The migrated prompt text in resources/prompts/ is the operator's own writing,
carried byte for byte out of the legacy application. Files whose checksum
entry says "drafted" were written in this repository at the operator's
direction, for modes the legacy application never had; the operator edits
them freely, and an edit is recorded by updating the checksum. Either way the
text is not edited to suit the code: the code fills declared placeholders and
nothing else.
"""
