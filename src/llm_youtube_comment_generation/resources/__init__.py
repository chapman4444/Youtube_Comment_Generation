"""Versioned prompt text and declarative option definitions.

The prompt text in resources/prompts/ is the operator's own writing, migrated
byte for byte out of the legacy application. It is not edited to suit the
code: the code fills declared placeholders and nothing else.
"""
